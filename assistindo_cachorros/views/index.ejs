<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Firewall Intrusion</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #0d0d0d;
            font-family: 'Courier New', Courier, monospace;
            color: <% if (success) { %>
                #00ff55;
            <% } else { %>
                #ff4d4d;
            <% } %>;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            text-align: center;
            position: relative;
            z-index: 2;
        }
        .message {
            font-size: 1.5em;
            text-transform: uppercase;
            animation: flicker 2s infinite;
            margin-bottom: 20px;
        }
        .gif-container {
            margin: 20px 0;
        }
        .gif-container img {
            width: 300px;
            height: auto;
            border: 2px solid <% if (success) { %>
                #00ff55;
            <% } else { %>
                #ff4d4d;
            <% } %>
            box-shadow: 0 0 15px <% if (success) { %>
                #00ff55;
            <% } else { %>
                #ff4d4d;
            <% } %>;
        }
        .sub-message {
            font-size: 1em;
        }
        .error {
            color: #ff4d4d;
            text-shadow: 0 0 10px #ff4d4d, 0 0 20px #ff4d4d;
        }
        .success {
            color: #00ff55;
            text-shadow: 0 0 10px #00ff55, 0 0 20px #00ff55;
        }
        .background-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, <% if (success) { %>
                rgba(0, 255, 85, 0.2)
            <% } else { %>
                rgba(255, 77, 77, 0.2)
            <% } %> 0%, rgba(0, 0, 0, 0.8) 100%);
            z-index: 1;
            animation: pulse 5s infinite;
        }
        @keyframes flicker {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
        }
        @keyframes pulse {
            0% { opacity: 0.5; }
            50% { opacity: 0.8; }
            100% { opacity: 0.5; }
        }
    </style>
</head>
<body>
    <div class="background-overlay"></div>
    <div class="container">
        <div class="message">
            <% if (success) { %>
                <p class="success">ACESSO PERMITIDO: Bem-vindo ao DedSec</p>
            <% } else { %>
                <p class="error">ACESSO NEGADO: Invasor Detectado</p>
            <% } %>
        </div>
        <div class="gif-container">
            <% if (success) { %>
                <img src="dedsec.jpg" alt="Intruder">
            <% } else { %>
                <img src="intruder.gif" alt="Intruder">
            <% } %>
        </div>
        <div class="sub-message">
            <% if (success) { %>
                <pre class="success"><%= message %></pre>
            <% } else { %>
                <pre class="error"><%= message %></pre>
            <% } %>
        </div>
    </div>
    <% if (locals.code) { %>
        <!-- Header Secret-Code: AidenPearce -->
    <% } %>
</body>
</html>